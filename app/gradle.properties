# Enable AndroidX support
android.useAndroidX=true
android.enableJetifier=true

# Optional: Customize Gradle settings for performance (adjust as necessary)
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true

# Set JVM heap size (adjust depending on the available system memory)
org.gradle.jvmargs=-Xmx2048m

# Enable Gradle's new configuration cache (recommended for Gradle 7+)
org.gradle.configuration-cache=true

# Set the Gradle version (if necessary, adjust for your project)
distributionUrl=https\://services.gradle.org/distributions/gradle-8.0-all.zip

# Enable additional lint checks (optional)
lintOptions.abortOnError=false

CLIENT_SECRET=szTfCajigkrFeYMjZYPBf31KqlMnAT7h

